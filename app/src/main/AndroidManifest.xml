<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.walts.ideas" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/light_bulb"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".activities.ListIdeasActivity"
            android:label="@string/title_activity_list_ideas" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.CreateIdeaActivity"
            android:label="@string/title_activity_create_idea" >
        </activity>

        <activity
            android:name=".activities.EditIdeaActivity"
            android:label="@string/title_activity_edit_idea" >
        </activity>

        <activity
            android:name=".activities.ViewIdeaActivity"
            android:label="@string/title_activity_view_idea" >
        </activity>

    </application>

</manifest>
